open this folder in vscode and open terminal and execute the command `pip install -r requirements.txt`
this will install all necessary modules required for this project.

after installation of modules is completed, execute `jupyter notebook` in terminal.

A new browser window will open and there open the "AmazonAnalysis.ipynb" notebook.

Now you can execute the program.